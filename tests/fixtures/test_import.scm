;;;; Test module for import validation
;;;; Defines a few simple symbols for testing ⋖

;; Simple arithmetic functions
(≔ add (λ (x y) (⊕ x y)))
(≔ multiply (λ (x y) (⊗ x y)))
(≔ subtract (λ (x y) (⊖ x y)))
