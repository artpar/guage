;;;
;;; Use trace correctly to debug
;;;

(≔ id (λ (x) (⟲ x)))

;;; Test with quoted value
(≋ :test-quoted)
(≔ result (id (⌜ (a b c))))
(≋ result)

;;; Test with non-quoted list
(≋ :test-non-quoted)
(≔ result2 (id (⟨⟩ #1 #2)))
(≋ result2)
