; Debug test structure

; Generate tests
(define tests (doc-tests (quote +)))

; Show structure
trace :all-tests
tests

trace :first-test
(define first (car tests))
first

trace :test-symbol
(car first)

trace :rest
(cdr first)

trace :name
(car (cdr first))

trace :expected
(car (cdr (cdr first)))

trace :actual
(car (cdr (cdr (cdr first))))
