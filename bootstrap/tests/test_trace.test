;;;
;;; Use trace to debug
;;;

(define id (lambda (x) (trace :in-closure x)))

;;; Test with quoted value
(print :test-quoted)
(define result (id (quote (a b c))))
(print result)

;;; Test with non-quoted list
(print :test-non-quoted)
(define result2 (id (cons #1 #2)))
(print result2)
