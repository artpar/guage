; Test suite for source introspection (⊛)
; Verifies that ⊛ returns lambda body correctly

(≋ "Testing Source Introspection...")

; Lambda source retrieval
(≔ my-fn (λ (x) (⊕ x #1)))

; ⊛ returns the body of a lambda
(⊨ :lambda-source-is-pair
   #t
   (⟨⟩? (⊛ my-fn)))

; ⊛ on non-lambda returns error
(⊨ :source-non-lambda-error
   #t
   (⚠? (⊛ #42)))

(≋ "\nSource introspection tests complete.")
