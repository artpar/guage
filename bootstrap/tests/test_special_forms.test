;;;
;;; Test if other special forms have the same issue
;;;

(≔ id (λ (x) x))

;;; Test 1: Quote
(≋ :direct-quote)
(≋ (⌜ (a b c)))

(≋ :quote-through-closure)
(≋ (id (⌜ (a b c))))

;;; Test 2: Cons (primitive, not special form)
(≋ :direct-cons)
(≋ (⟨⟩ :a :b))

(≋ :cons-through-closure)
(≋ (id (⟨⟩ :a :b)))

;;; Test 3: If (special form)
(≋ :direct-if)
(≋ (? #t #42 #99))

(≋ :if-through-closure)
(≋ (id (? #t #42 #99)))
